/**
 * @lms/config — shared configuration entry point.
 * Consumers should import the specific preset file, e.g.:
 *   require('@lms/config/eslint/nextjs')
 *   require('@lms/config/tailwind/preset')
 */
module.exports = {};
